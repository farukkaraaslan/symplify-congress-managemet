window.Symplify = window.Symplify || {};
window.Symplify.DataTables = window.Symplify.DataTables || {};

window.Symplify.DataTables.defaults = {
    processing: true,
    serverSide: true,
    searching: true,
    ordering: true,
    paging: true,
    pageLength: 10,
    autoWidth: false,
    responsive: true
};
