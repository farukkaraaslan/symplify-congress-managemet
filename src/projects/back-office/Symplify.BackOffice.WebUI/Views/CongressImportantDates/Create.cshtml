@model Symplify.BackOffice.Application.Features.CongressImportantDates.Commands.Create.CreateCongressImportantDateCommand

@{
    ViewData["Title"] = "Congress Important Dates Oluştur";
    string? currentCulture = Context.Request.RouteValues["culture"]?.ToString();
}

<div class="dashboard-main-body">
    <div class="card border shadow-none radius-12">
        <div class="card-header border-bottom">
            <h6 class="text-lg mb-0">Congress Important Dates Oluştur</h6>
        </div>

        <div class="card-body">
            <form asp-action="Create" asp-route-culture="@currentCulture" method="post" autocomplete="off">
                @Html.AntiForgeryToken()

                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="row gy-4">
            <div class="col-md-6">
                <label class="form-label" asp-for="CongressId"></label>
                <input class="form-control" asp-for="CongressId" />
                <span asp-validation-for="CongressId" class="text-danger"></span>
            </div>
            <div class="col-md-6">
                <label class="form-label" asp-for="Date"></label>
                <input class="form-control" asp-for="Date" type="datetime-local" />
                <span asp-validation-for="Date" class="text-danger"></span>
            </div>
            <div class="col-md-6">
                <label class="form-label" asp-for="Order"></label>
                <input class="form-control" asp-for="Order" type="number" />
                <span asp-validation-for="Order" class="text-danger"></span>
            </div>
            <div class="col-md-6">
                <label class="form-label d-block" asp-for="IsActive"></label>
                <input type="hidden" name="IsActive" value="false" />
                <div class="form-check form-switch">
                    <input class="form-check-input" asp-for="IsActive" value="true" />
                    <label class="form-check-label" asp-for="IsActive"></label>
                </div>
                <span asp-validation-for="IsActive" class="text-danger"></span>
            </div>
            <div class="col-12">
                <div class="alert alert-warning mb-0">
                    Bu form iskeleti karmaşık alanları otomatik render etmez: <strong>Translations</strong>.
                    Translation/child collection alanları için mevcut feature standardına göre özel partial eklenmelidir.
                </div>
            </div>
                </div>

                <div class="d-flex justify-content-end gap-2 mt-24">
                    <a asp-action="Index" asp-route-culture="@currentCulture" class="btn btn-outline-neutral-900 radius-8 px-20 py-11">Vazgeç</a>
                    <button type="submit" class="btn btn-primary-600 radius-8 px-20 py-11">Kaydet</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
